<!DOCTYPE html>
<html>
<head>
  <title>Carrito</title>
</head>
<body>
  <h1>Carrito</h1>
  <ul>
    {{#each cart.products}}
      <li>
        <h2>{{this.product.nombre}}</h2>
        <p>Cantidad: {{this.quantity}}</p>
      </li>
    {{/each}}
  </ul>
</body>
</html>
